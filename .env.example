# Checked — environment variables
# Copy to .env.local for local dev; set the same keys in Vercel Project Settings → Environment Variables.
# v0 needs NONE of these (state is localStorage-only). They're placeholders for upcoming phases:

# Phase 2 — Database (Neon Postgres) + Auth (Clerk)
# DATABASE_URL=
# NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
# CLERK_SECRET_KEY=

# Phase 4 — The Tower (Anthropic Claude API for routed RAG)
# ANTHROPIC_API_KEY=

# Phase 5 — Duty-Free (affiliate)
# NEXT_PUBLIC_AMAZON_ASSOCIATES_TAG=
